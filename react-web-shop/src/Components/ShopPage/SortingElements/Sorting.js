import "./Sorting.css";

const Sorting = () => {
  /*
  const [showList, setShowList] = useState(false);
  const showListHandler = () => {
    if (!showList) {
      setShowList(true);
    } else {
      setShowList(false);
    }
  };*/

  return (
    <div className="sort">
      <div className="main">
        <button>Sorting by</button>
      </div>
    </div>
  );
};
export default Sorting;
//{`main-list ${!showList ? "active" : ""}`}
/*<div className="main-list">
        <a href="#">Sort by Price/High to Low</a>
        <a href="#">Sort by Price/Low to High</a>
      </div>
*/
